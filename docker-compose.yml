services:
  app:
    container_name: template-mizu-django
    build:
      context: .
      dockerfile: ./dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./:/app
      - ./data/web/static:/data/web/static
      - ./data/web/media:/data/web/media
    env_file:
      - ./dotenv_files/.env
    depends_on:
      - database
    networks:
      - template_network
  database:
    container_name: template-mizu-django-database
    image: postgres:14-alpine
    volumes:
      - ./data/postgres/data:/var/lib/postgresql/data/
    env_file:
      - ./dotenv_files/.env
    ports:
      - 5432:5432
    networks:
      - template_network
networks:
  template_network:
    driver: bridge
